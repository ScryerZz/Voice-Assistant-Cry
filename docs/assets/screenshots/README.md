# Screenshot Assets

Put UI screenshots here using the names listed in `docs/SCREENSHOTS.md`.

Do not commit screenshots that expose API keys, private paths, private notes, or command history that should stay local.
